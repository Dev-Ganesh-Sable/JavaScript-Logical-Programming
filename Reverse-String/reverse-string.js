
let str = "ganesh"
let newStr = ""

// using for loop reverse string
for (let i = str.length - 1; i >= 0; i--) {
    // console.log(str[i]);

    // assign to value new str veriable
    newStr += str[i]
}
console.log(newStr);



